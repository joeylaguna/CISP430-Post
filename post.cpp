#include <iostream>
using namespace std;

int clean()
{
    char comments;
    while (in.peek()=='\n' || in.peek()=='\n')
}

int main()
{
    char x[50];
    while (clean() && in.good)
    {
        in>>x;
    }
    return 0;
}
